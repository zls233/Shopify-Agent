# Shopify Agent 工作约定

本仓库是执行 Shopify 建站流程的 CLI，不是某个店铺的主题目录。开始工作前，先阅读 `README.md`、`cli.mjs`、`src/steps.mjs`、`src/runner.mjs` 和相关提示词；检查 Git 状态及 `npm run status`，确认本次任务的目标项目、原站 URL、店铺和执行范围。

## 使用系统执行任务

- 用户要求“使用 Shopify Agent”时，以本仓库的 CLI 和步骤状态为执行入口。仅阅读提示词、参考步骤顺序或在另一个目录手工编写主题，不等于运行了 Shopify Agent。
- 执行前用 `npm run plan` 列出当前模式会运行、跳过和无法运行的步骤。按 `src/steps.mjs` 定义的顺序逐步执行提示词；完成一步后核对结构化结果、实际产物与验收条件，再进入下一步。
- 使用 `.shopify-agent/` 中的状态、会话 ID、提示词哈希和事件记录证明步骤确实运行。报告中区分“系统执行”“手工执行”“仅阅读提示词”和“尚未执行”，不得把一种表述成另一种。
- 优先恢复已绑定项目的未完成步骤；不要无故重做已验收步骤，或把成果另放在未绑定的目录后宣称系统已完成该任务。

## 当前能力边界

- `full-store` 模式需要有效店铺、Admin 授权、Theme Access 和已登录的指定浏览器，包含远端写入及后续自动发布。`theme-only` 模式无需店铺，只运行本地参考采集、主题实现和静态 QA；按任务范围选择模式，不得用完整建店流程冒充仅主题执行。
- `theme-only` 的 `completed_local` 只表示本地阶段完成。接管已有项目并使用 `--from` 时，先前步骤标为 `external`，不得说成系统已执行。不得声称主题已通过 Shopify 预览或购买路径验收。
- 没有店铺时可以完成原站采集、主题源码和静态检查。Shopify Liquid 的真实渲染、动态商品、购物袋、桌面及手机预览，应标记为待店铺验证。

## 店铺与产物边界

- 每次远端操作都确认准确的店铺、App 身份、主题 ID 和当前主题角色。按 `README.md` 的授权方式处理 Theme Access 与 Admin 权限；不得从浏览器会话提取凭据，也不要在日志或报告中输出密钥。
- 主题开发与商品、内容、发布是不同阶段。只做主题时，不导入商品、不修改商家内容、不发布主题，也不开放店铺。完整流程的远端步骤必须遵守现有前置检查和回读验收。
- 保留用户现有文件和未提交改动。新建或接管目标项目目录前，核对目录内容和 Git 状态；不要静默覆盖。最终明确列出 Agent 仓库内的改动和生成项目中的改动。
- 不用虚构的价格、运费、退货政策或可能指向 404 的链接证明页面已完成；无真实数据时应标明占位状态及待绑定资源。

## 验证与汇报

- 修改 Agent 代码后运行 `npm run check` 和 `npm test`；为新增模式补充覆盖步骤顺序、跳过远端操作、断点续跑和失败状态的测试。
- `shopify theme check` 只证明主题静态检查结果。原站截图不能代替生成主题的截图；浏览器视觉、交互、远端文件一致性和公开访问须分别报告证据与未验证项。
- 汇报本次实际运行的命令、各步骤状态、产物路径、验证结果和阻塞原因。不要以“文件已写完”代替流程完成，也不要把模拟测试通过表述成真实店铺端到端通过。
