# Copy to .env.local. Never commit real values.
SHOPIFY_CLI_THEME_TOKEN=
# Codex CLI normally reuses its saved authentication.
# Keep Shopify Admin credentials in the generated project's ignored .env.local if used.
